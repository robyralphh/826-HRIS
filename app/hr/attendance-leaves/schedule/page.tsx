import ScheduleView from '@/views/Schedule';

export const metadata = {
    title: 'Schedules | HRIS',
    description: 'Manage employee shifts and schedules',
};

export default function SchedulePage() {
    return <ScheduleView />;
}
